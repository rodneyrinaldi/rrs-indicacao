services:
  # Sem postgres — banco e o Supabase (externo, ja existente)

  nextjs:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: r2-nextjs
    restart: unless-stopped
    env_file: .env.prod.supabase
    ports:
      - "3000:3000"
    networks: [r2-net]
    # Sem depends_on — banco e externo

  # python:
  #   build: ./apps/python
  #   container_name: r2-python
  #   restart: unless-stopped
  #   env_file: .env.prod.supabase
  #   networks: [r2-net]

  # rust:
  #   build: ./apps/rust
  #   container_name: r2-rust
  #   restart: unless-stopped
  #   env_file: .env.prod.supabase
  #   networks: [r2-net]

  # nginx:
  #   image: nginx:alpine
  #   container_name: r2-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - certdata:/etc/letsencrypt
  #   networks: [r2-net]
  #   depends_on: [nextjs]
